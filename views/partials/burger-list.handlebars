<div class="card">
    <div class="card-header text-center">
        <h4>Devoured</h4>
    </div>
    <div class="card-body scroll">

        {{#each devouredList}}
            <div>
                <p>{{id}}. {{burger_name}}
                    <button class="btn btn-sm btn-dark m-1 delete-btn float-right"
                            data-id="{{this.id}}">Delete <i class="fas fa-times"></i>
                    </button>
                    </p>
                </div>
        {{/each }}

    </div>
</div>